2.1 Compare to an array parameter. What do you prefer in this case?
I would prefer using the varargs parameter because it’s more flexible and convenient. It allows adding one or multiple DVDs without needing to create an array manually.